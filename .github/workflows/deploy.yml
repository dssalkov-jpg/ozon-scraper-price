name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: SSH deploy
        uses: appleboy/ssh-action@v1
        with:
          host: 109.73.192.175
          username: root
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 22
          script: |
            /usr/local/bin/deploy_ozon.sh
